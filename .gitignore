# ============================================================
# 自分仕様(拠点ローカル) —— この下のものは絶対にコミットされない
# ============================================================

# 環境定義(誰のクルーとして動くか・前提条件・パス・編成)
identity.yaml

# APIキー・通知先などのシークレット
machines/*.env
.env
.env.*

# ============================================================
# 実行時データ(拠点ごとに別。共有すると衝突する)
# ============================================================
state/
logs/

# ============================================================
# 生成物
# ============================================================
# identity.yaml から自動生成される Claude Code ローカル設定
.claude/settings.local.json

# クルー実行環境の設定(プライベート化方針 2026-09-01。roster.coder=codex なら gen_settings.py が生成)
.mcp.json

dist/
__pycache__/
*.pyc
.DS_Store
